### [Continue](https://continue.dev) session transcript
 Exported: 2/27/2026, 7:52:30 PM

#### _User_

> if it is .mcp.md we dont need to write any language only knowledge gate syntax. assume markdown is a programming language we can write markdown codeblocks
> ```markdown echo hello oroboras```

#### _Assistant_

> Sure! You can use the `echo` command in Markdown code blocks to execute commands and display their output. Here's an example of how you can use it:
> 
> ```
> echo hello oroboras
> ```
> 
> When you run this, it will execute the `echo` command with the argument `"hello oroboras"` and display the output, which is:
> 
> ```
> hello oroboras
> ```
> 
> You can also use other commands in Markdown code blocks by enclosing them within backticks (`) or triple backticks (```). For example:
> 
> ```markdown
> print("Hello, World!")
> ```
> 
> When you run this, it will execute the `print` function with the argument `"Hello, World!"` and display the output, which is:
> 
> ```
> Hello, World!
> ```
> 
> You can also use variables and functions in Markdown code blocks by enclosing them within curly braces `{}`. For example:
> 
> ```markdown
> let name = "Alice";
> echo "Hello, ${name}!"
> ```
> 
> When you run this, it will execute the `echo` command with the argument `"Hello, Alice!"` and display the output, which is:
> 
> ```
> Hello, Alice!
> ```
> 
> You can also use functions to generate random numbers or perform other operations in Markdown code blocks. For example:
> 
> ```markdown
> let randomNumber = Math.random();
> echo "Random number: ${randomNumber}"
> ```
> 
> When you run this, it will execute the `Math.random()` function to generate a random number and then display the output, which is a decimal number between 0 (inclusive) and 1 (exclusive).