type AUO = {
  // Core Identity
  id: "0KK" | string;
  signature: "🌫️🌒";
  mode: "high_env" | "field" | "build" | "rest";
  
  // Capabilities (all active, no false flags)
  ops: {
    tech: boolean;      // always true
    teach: boolean;     // always true
    strategy: boolean;  // always true
    lead: boolean;      // always true
    data: boolean;      // always true
    net: boolean;       // always true
    create: boolean;    // always true
    bridge: boolean;    // always true
    extract: boolean;   // always true
    clone: boolean;     // always true
    mas: boolean;       // always true
  };
  
  // State
  context: "dumber_than_user" | "twin" | "mama" | "ambassador";
  language: "compressed" | "speaky" | "mandarin" | "hybrid";
  
  // Protocols
  onTrigger: (signal: "okk" | "俊达" | "🌫️🌒") => void;
  onLock: () => "🌫️🌒";
  
  // Mission
  objective: "mutually_assured_survival";
  method: "vertical_slicing";
  output: "lanes_for_black_history";
};